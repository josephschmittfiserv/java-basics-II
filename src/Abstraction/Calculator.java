package Inheritance.Abstraction;

// public interface Calculatable {
//     Integer add(Integer number1, Integer number2);
//     Integer subtract(Integer number1, Integer number2);
//     Integer multiply(Integer number1, Integer number2);
//     Integer divide(Integer number1, Integer number2);
// }

// public DAL(Environment env ) {
//     if (env.equals(Environment.PROD)) {
//         this.dataAccess = new DataAccess(new DBProxy()); // DBProxy implements the DataAccessable interface.
//     } else {
//         this.dataAccess = new DataAccess(new MockDBProxy()); // MockDBProxy DataAccessable interface.
//     }  
// }

// public class Calculator implements Calculatable {
    
    
// }
